apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: keycloak-tls
  namespace: keycloak
spec:
  secretName: keycloak-tls
  issuerRef:
    name: local-ca-cluster-issuer  # From Guide 1
    kind: ClusterIssuer
  dnsNames:
    - keycloak.local